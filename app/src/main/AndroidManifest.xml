<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yahia.tourguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/restaurants_activity_label"
            android:name=".RestaurantActivity">
        </activity>
        <activity android:name=".AttractionsActivity"
            android:label="@string/attractions_activity_label">
        </activity>
        <activity android:name=".HotelsActivity"
            android:label="@string/hotels_activity_name">
        </activity>
        <activity android:name=".MallsActivity"
            android:label="@string/malls_activity_name"/>
    </application>

</manifest>